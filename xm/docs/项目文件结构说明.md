# 项目文件结构说明文档

## 📋 目录
- [项目根目录](#项目根目录)
- [前端源码目录 (src/)](#前端源码目录-src)
- [后端 API 目录 (api/)](#后端-api-目录-api)
- [脚本目录 (scripts/)](#脚本目录-scripts)
- [文档目录 (docs/)](#文档目录-docs)
- [配置文件说明](#配置文件说明)
- [静态资源目录](#静态资源目录)

## 🏠 项目根目录

```
dldzxx_web/
├── .gitignore              # Git 忽略文件配置
├── .trae/                  # Trae IDE 配置目录
├── .vite/                  # Vite 缓存目录
├── .vscode/                # VS Code 配置目录
├── README.md               # 项目说明文档
├── api/                    # 后端 API 服务目录
├── docs/                   # 项目文档目录
├── index.html              # HTML 入口文件
├── nodemon.json            # Nodemon 配置文件
├── package.json            # 项目依赖和脚本配置
├── pnpm-lock.yaml          # pnpm 锁定文件
├── postcss.config.js       # PostCSS 配置
├── public/                 # 静态资源目录
├── scripts/                # 项目脚本目录
├── src/                    # 前端源码目录
├── tailwind.config.js      # Tailwind CSS 配置
├── tsconfig.json           # TypeScript 配置
├── vercel.json             # Vercel 部署配置
└── vite.config.ts          # Vite 构建配置
```

### 核心配置文件

| 文件名 | 作用 | 说明 |
|--------|------|------|
| `package.json` | 项目配置 | 定义项目依赖、脚本命令和元信息 |
| `vite.config.ts` | 构建配置 | Vite 构建工具的配置文件 |
| `tsconfig.json` | TypeScript 配置 | TypeScript 编译器配置 |
| `tailwind.config.js` | 样式配置 | Tailwind CSS 框架配置 |
| `vercel.json` | 部署配置 | Vercel 平台部署配置 |

## 🎨 前端源码目录 (src/)

```
src/
├── App.vue                 # 根组件
├── main.ts                 # 应用入口文件
├── components/             # 公共组件目录
│   ├── common/            # 通用组件
│   ├── charts/            # 图表组件
│   └── layout/            # 布局组件
├── pages/                 # 页面组件目录
│   ├── Dashboard/         # 数据大屏页面
│   ├── Employees/         # 员工管理页面
│   ├── Devices/           # 设备管理页面
│   └── AiChat/            # AI 助手页面
├── router/                # 路由配置
│   └── index.ts           # 路由定义文件
├── stores/                # 状态管理
│   ├── index.ts           # Store 入口
│   ├── dashboard.ts       # 数据大屏状态
│   ├── employees.ts       # 员工管理状态
│   └── devices.ts         # 设备管理状态
├── types/                 # TypeScript 类型定义
│   ├── index.ts           # 通用类型
│   ├── api.ts             # API 相关类型
│   └── components.ts      # 组件相关类型
├── utils/                 # 工具函数
│   ├── index.ts           # 工具函数入口
│   ├── api.ts             # API 请求工具
│   ├── format.ts          # 格式化工具
│   └── constants.ts       # 常量定义
├── composables/           # 组合式函数
│   ├── useApi.ts          # API 请求 Hook
│   ├── useChart.ts        # 图表 Hook
│   └── useWebSocket.ts    # WebSocket Hook
├── assets/                # 静态资源
│   ├── images/            # 图片资源
│   ├── icons/             # 图标资源
│   └── styles/            # 样式文件
└── style.css              # 全局样式文件
```

### 页面组件详细说明

#### Dashboard/ (数据大屏)
```
Dashboard/
├── DashboardPage.vue      # 数据大屏主页面
├── components/            # 页面专用组件
│   ├── NetworkStatus.vue  # 网络状态组件
│   ├── AlertPanel.vue     # 告警面板组件
│   ├── StatisticsCard.vue # 统计卡片组件
│   └── RealTimeChart.vue  # 实时图表组件
└── composables/           # 页面专用 Hook
    └── useDashboard.ts    # 数据大屏逻辑
```

#### Employees/ (员工管理)
```
Employees/
├── EmployeePage.vue       # 员工管理主页面
├── components/            # 页面专用组件
│   ├── EmployeeCard.vue   # 员工卡片组件
│   ├── EmployeeTable.vue  # 员工表格组件
│   ├── EmployeeDialog.vue # 员工详情弹窗
│   └── FilterPanel.vue    # 筛选面板组件
└── composables/           # 页面专用 Hook
    └── useEmployees.ts    # 员工管理逻辑
```

#### Devices/ (设备管理)
```
Devices/
├── DevicePage.vue         # 设备管理主页面
├── components/            # 页面专用组件
│   ├── DeviceCard.vue     # 设备卡片组件
│   ├── DeviceStatus.vue   # 设备状态组件
│   ├── DeviceChart.vue    # 设备图表组件
│   └── DeviceConfig.vue   # 设备配置组件
└── composables/           # 页面专用 Hook
    └── useDevices.ts      # 设备管理逻辑
```

#### AiChat/ (AI 助手)
```
AiChat/
├── AiChatPage.vue         # AI 助手主页面
├── components/            # 页面专用组件
│   ├── ChatWindow.vue     # 聊天窗口组件
│   ├── MessageItem.vue    # 消息项组件
│   ├── InputPanel.vue     # 输入面板组件
│   └── SuggestionList.vue # 建议列表组件
└── composables/           # 页面专用 Hook
    └── useChat.ts         # 聊天逻辑
```

## 🔧 后端 API 目录 (api/)

```
api/
├── server.ts              # 服务器入口文件
├── routes/                # API 路由目录
│   ├── index.ts           # 路由入口
│   ├── dashboard.ts       # 数据大屏 API
│   ├── employees.ts       # 员工管理 API
│   ├── devices.ts         # 设备管理 API
│   └── chat.ts            # AI 聊天 API
├── middleware/            # 中间件目录
│   ├── cors.ts            # CORS 中间件
│   ├── auth.ts            # 认证中间件
│   └── logger.ts          # 日志中间件
├── utils/                 # 后端工具函数
│   ├── response.ts        # 响应格式化
│   ├── validation.ts      # 数据验证
│   └── database.ts        # 数据库工具
├── types/                 # 后端类型定义
│   ├── api.ts             # API 类型
│   ├── database.ts        # 数据库类型
│   └── common.ts          # 通用类型
└── data/                  # 模拟数据
    ├── employees.json     # 员工数据
    ├── devices.json       # 设备数据
    └── dashboard.json     # 大屏数据
```

### API 路由说明

| 路由文件 | 功能 | 主要端点 |
|----------|------|----------|
| `dashboard.ts` | 数据大屏 API | `/api/dashboard/stats`, `/api/dashboard/alerts` |
| `employees.ts` | 员工管理 API | `/api/employees`, `/api/employees/:id` |
| `devices.ts` | 设备管理 API | `/api/devices`, `/api/devices/:id/status` |
| `chat.ts` | AI 聊天 API | `/api/chat/message`, `/api/chat/history` |

## 📜 脚本目录 (scripts/)

```
scripts/
├── dev.sh                 # 开发环境启动脚本
├── build.sh               # 生产构建脚本
├── deploy.sh              # 部署脚本
├── setup.sh               # 项目初始化脚本
└── test.sh                # 测试脚本
```

### 脚本功能说明

| 脚本名 | 功能 | 使用场景 |
|--------|------|----------|
| `dev.sh` | 启动开发环境 | 本地开发时使用 |
| `build.sh` | 构建生产版本 | 部署前构建 |
| `deploy.sh` | 部署到 Vercel | 生产环境部署 |
| `setup.sh` | 初始化项目 | 首次安装时使用 |
| `test.sh` | 运行测试 | 代码质量检查 |

## 📚 文档目录 (docs/)

```
docs/
├── ARCHITECTURE.md        # 项目架构说明
├── FILE_STRUCTURE.md      # 文件结构说明 (本文档)
├── API.md                 # API 接口文档
├── DEVELOPMENT.md         # 开发指南
├── DEPLOYMENT.md          # 部署指南
└── FAQ.md                 # 常见问题解答
```

## ⚙️ 配置文件说明

### package.json
```json
{
  "name": "dldzxx_web",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "preview": "vite preview",
    "server": "nodemon",
    "dev:all": "concurrently \"pnpm run dev\" \"pnpm run server\""
  },
  "dependencies": {
    "vue": "^3.x",
    "vue-router": "^4.x",
    "tdesign-vue-next": "^1.x",
    "echarts": "^5.x"
  }
}
```

### vite.config.ts
```typescript
export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, 'src')
    }
  },
  server: {
    proxy: {
      '/api': 'http://localhost:3001'
    }
  }
})
```

### tsconfig.json
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "node",
    "strict": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  }
}
```

### tailwind.config.js
```javascript
module.exports = {
  content: ['./index.html', './src/**/*.{vue,js,ts}'],
  theme: {
    extend: {
      colors: {
        primary: '#0052d9',
        success: '#00a870',
        warning: '#ed7b2f',
        error: '#d54941'
      }
    }
  }
}
```

## 🖼️ 静态资源目录

### public/
```
public/
├── favicon.ico            # 网站图标
├── logo.png               # 应用 Logo
├── images/                # 公共图片
│   ├── bg-dashboard.jpg   # 大屏背景图
│   ├── avatar-default.png # 默认头像
│   └── icons/             # 图标文件
└── data/                  # 静态数据文件
    └── mock-data.json     # 模拟数据
```

### src/assets/
```
src/assets/
├── images/                # 组件图片资源
│   ├── login-bg.jpg       # 登录背景
│   ├── error-404.svg      # 404 页面图片
│   └── empty-state.svg    # 空状态图片
├── icons/                 # SVG 图标
│   ├── dashboard.svg      # 大屏图标
│   ├── employees.svg      # 员工图标
│   ├── devices.svg        # 设备图标
│   └── chat.svg           # 聊天图标
└── styles/                # 样式文件
    ├── global.css         # 全局样式
    ├── variables.css      # CSS 变量
    └── components.css     # 组件样式
```

## 🔍 文件命名规范

### 组件文件
- **页面组件**: `PascalCase.vue` (如 `DashboardPage.vue`)
- **业务组件**: `PascalCase.vue` (如 `EmployeeCard.vue`)
- **通用组件**: `PascalCase.vue` (如 `LoadingSpinner.vue`)

### 脚本文件
- **工具函数**: `camelCase.ts` (如 `formatDate.ts`)
- **类型定义**: `camelCase.ts` (如 `apiTypes.ts`)
- **配置文件**: `kebab-case.js` (如 `vite.config.ts`)

### 样式文件
- **全局样式**: `kebab-case.css` (如 `global.css`)
- **组件样式**: `PascalCase.module.css` (如 `Button.module.css`)

### 资源文件
- **图片文件**: `kebab-case.ext` (如 `login-bg.jpg`)
- **图标文件**: `kebab-case.svg` (如 `user-icon.svg`)

## 📝 文件作用总结

### 关键文件功能

| 文件/目录 | 主要功能 | 重要性 |
|-----------|----------|--------|
| `src/main.ts` | 应用入口，初始化 Vue 应用 | ⭐⭐⭐⭐⭐ |
| `src/App.vue` | 根组件，应用整体结构 | ⭐⭐⭐⭐⭐ |
| `src/router/` | 路由配置，页面导航 | ⭐⭐⭐⭐⭐ |
| `src/pages/` | 页面组件，核心业务逻辑 | ⭐⭐⭐⭐⭐ |
| `api/server.ts` | 后端服务入口 | ⭐⭐⭐⭐⭐ |
| `vite.config.ts` | 构建配置 | ⭐⭐⭐⭐ |
| `package.json` | 项目配置和依赖 | ⭐⭐⭐⭐ |
| `scripts/` | 自动化脚本 | ⭐⭐⭐ |

---

本文档详细说明了项目的文件结构和各文件的作用，有助于开发者快速理解项目组织方式。