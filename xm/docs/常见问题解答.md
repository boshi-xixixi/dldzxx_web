# 常见问题解答 (FAQ)

## 📋 目录
- [安装和环境问题](#安装和环境问题)
- [开发相关问题](#开发相关问题)
- [构建和部署问题](#构建和部署问题)
- [功能使用问题](#功能使用问题)
- [性能和优化问题](#性能和优化问题)
- [故障排除](#故障排除)

## 🔧 安装和环境问题

### Q: 如何安装和启动项目？

**A:** 请按照以下步骤操作：

1. **检查环境要求**
   - Node.js >= 18.0.0
   - pnpm >= 8.0.0 (推荐)

2. **克隆并初始化项目**
   ```bash
   git clone <repository-url>
   cd dldzxx_web
   ./scripts/setup.sh
   ```

3. **启动开发服务器**
   ```bash
   ./scripts/dev.sh
   ```

### Q: 为什么推荐使用 pnpm 而不是 npm？

**A:** pnpm 相比 npm 有以下优势：
- **更快的安装速度**: 使用硬链接和符号链接
- **更少的磁盘空间**: 避免重复安装相同的包
- **更严格的依赖管理**: 避免幽灵依赖问题
- **更好的 monorepo 支持**: 适合大型项目

### Q: Node.js 版本要求为什么是 18+？

**A:** 项目使用了以下需要 Node.js 18+ 的特性：
- ES2022 语法支持
- 原生 fetch API
- 更好的 TypeScript 支持
- 性能优化和安全更新

### Q: 如何在 Windows 系统上运行项目？

**A:** Windows 用户可以：
1. 使用 WSL2 (推荐)
2. 使用 Git Bash 运行脚本
3. 直接使用 npm 命令：
   ```bash
   npm install
   npm run dev:all
   ```

## 💻 开发相关问题

### Q: 如何添加新的页面？

**A:** 按照以下步骤添加新页面：

1. **创建页面组件**
   ```bash
   # 在 src/pages/ 下创建新目录
   mkdir src/pages/NewPage
   touch src/pages/NewPage/NewPage.vue
   ```

2. **添加路由配置**
   ```typescript
   // src/router/index.ts
   {
     path: '/new-page',
     name: 'NewPage',
     component: () => import('@/pages/NewPage/NewPage.vue')
   }
   ```

3. **更新导航菜单**
   ```vue
   <!-- 在侧边栏组件中添加菜单项 -->
   <t-menu-item value="new-page" to="/new-page">
     <template #icon><NewPageIcon /></template>
     新页面
   </t-menu-item>
   ```

### Q: 如何使用 TDesign 组件？

**A:** TDesign 组件已全局注册，可直接使用：

```vue
<template>
  <t-card>
    <t-button theme="primary" @click="handleClick">
      点击按钮
    </t-button>
    <t-table :data="tableData" :columns="columns" />
  </t-card>
</template>
```

参考 [TDesign Vue Next 文档](https://tdesign.tencent.com/vue-next/overview)

### Q: 如何添加新的 API 接口？

**A:** 在后端添加新接口：

1. **创建路由文件**
   ```typescript
   // api/routes/newModule.ts
   import express from 'express'
   const router = express.Router()
   
   router.get('/list', (req, res) => {
     res.json({
       code: 200,
       message: '获取成功',
       data: []
     })
   })
   
   export default router
   ```

2. **注册路由**
   ```typescript
   // api/server.ts
   import newModuleRoutes from './routes/newModule'
   app.use('/api/new-module', newModuleRoutes)
   ```

### Q: 如何使用 Tailwind CSS？

**A:** 项目已配置 Tailwind CSS，可直接使用工具类：

```vue
<template>
  <div class="bg-gray-100 p-4 rounded-lg shadow-md">
    <h1 class="text-2xl font-bold text-gray-800 mb-4">标题</h1>
    <p class="text-gray-600 leading-relaxed">内容文本</p>
  </div>
</template>
```

## 🏗️ 构建和部署问题

### Q: 构建时出现 TypeScript 错误怎么办？

**A:** 按以下步骤排查：

1. **检查类型错误**
   ```bash
   pnpm run check
   ```

2. **常见类型错误解决**
   ```typescript
   // 为未定义类型的变量添加类型注解
   const data: any[] = []
   
   // 使用类型断言
   const element = document.getElementById('app') as HTMLElement
   
   // 定义接口
   interface User {
     id: string
     name: string
   }
   ```

3. **忽略特定错误** (不推荐)
   ```typescript
   // @ts-ignore
   const result = someUntypedFunction()
   ```

### Q: 如何部署到 Vercel？

**A:** 项目已配置 Vercel 部署：

1. **使用脚本部署**
   ```bash
   ./scripts/deploy.sh
   ```

2. **手动部署**
   ```bash
   # 安装 Vercel CLI
   npm install -g vercel
   
   # 登录 Vercel
   vercel login
   
   # 部署
   vercel --prod
   ```

3. **配置环境变量**
   在 Vercel 控制台设置必要的环境变量

### Q: 构建后的文件太大怎么办？

**A:** 可以通过以下方式优化：

1. **分析打包大小**
   ```bash
   pnpm run build
   npx vite-bundle-analyzer dist
   ```

2. **代码分割**
   ```typescript
   // 路由懒加载
   const Dashboard = () => import('@/pages/Dashboard/DashboardPage.vue')
   
   // 动态导入
   const heavyLibrary = await import('heavy-library')
   ```

3. **移除未使用的代码**
   - 检查并删除未使用的依赖
   - 使用 Tree-shaking
   - 优化图片资源

## 🎯 功能使用问题

### Q: 数据大屏的数据是实时的吗？

**A:** 目前使用模拟数据，实际部署时需要：
1. 连接真实的数据源
2. 实现 WebSocket 或定时轮询
3. 配置数据更新频率

### Q: 如何自定义图表样式？

**A:** 项目使用 ECharts，可以自定义配置：

```typescript
const chartOptions = {
  title: {
    text: '自定义图表',
    textStyle: {
      color: '#333',
      fontSize: 16
    }
  },
  color: ['#0052d9', '#00a870', '#ed7b2f'],
  // 更多配置...
}
```

### Q: 员工管理页面如何添加新字段？

**A:** 需要同时修改前后端：

1. **更新类型定义**
   ```typescript
   interface Employee {
     id: string
     name: string
     newField: string // 新字段
   }
   ```

2. **更新表格列配置**
   ```typescript
   const columns = [
     { colKey: 'newField', title: '新字段', width: 120 }
   ]
   ```

3. **更新 API 接口**
   在后端 API 中添加对应字段的处理

### Q: AI 聊天功能如何接入真实的 AI 服务？

**A:** 目前是模拟实现，接入真实 AI 服务需要：

1. **选择 AI 服务提供商** (如 OpenAI、百度文心等)
2. **配置 API 密钥**
3. **修改聊天接口实现**
   ```typescript
   // api/routes/chat.ts
   import { OpenAI } from 'openai'
   
   const openai = new OpenAI({
     apiKey: process.env.OPENAI_API_KEY
   })
   
   router.post('/message', async (req, res) => {
     const response = await openai.chat.completions.create({
       model: 'gpt-3.5-turbo',
       messages: [{ role: 'user', content: req.body.message }]
     })
     // 处理响应...
   })
   ```

## ⚡ 性能和优化问题

### Q: 页面加载速度慢怎么办？

**A:** 可以从以下方面优化：

1. **代码分割和懒加载**
   ```typescript
   // 路由懒加载
   const routes = [
     {
       path: '/employees',
       component: () => import('@/pages/Employees/EmployeePage.vue')
     }
   ]
   ```

2. **图片优化**
   - 使用 WebP 格式
   - 实现图片懒加载
   - 压缩图片大小

3. **缓存策略**
   ```typescript
   // 使用 keep-alive 缓存组件
   <keep-alive>
     <router-view />
   </keep-alive>
   ```

### Q: 大量数据渲染卡顿怎么办？

**A:** 可以使用以下优化策略：

1. **虚拟滚动**
   ```vue
   <t-table
     :data="largeDataSet"
     :scroll="{ type: 'virtual', bufferSize: 20 }"
   />
   ```

2. **分页加载**
   ```typescript
   const { data, loading, loadMore } = usePagination(fetchData)
   ```

3. **防抖和节流**
   ```typescript
   import { debounce } from 'lodash-es'
   
   const handleSearch = debounce((keyword: string) => {
     // 搜索逻辑
   }, 300)
   ```

## 🔍 故障排除

### Q: 开发服务器启动失败

**A:** 检查以下问题：

1. **端口占用**
   ```bash
   # 检查端口占用
   lsof -i :5173
   lsof -i :3001
   
   # 杀死占用进程
   kill -9 <PID>
   ```

2. **依赖问题**
   ```bash
   # 清除缓存重新安装
   rm -rf node_modules pnpm-lock.yaml
   pnpm install
   ```

3. **权限问题**
   ```bash
   # 给脚本执行权限
   chmod +x scripts/*.sh
   ```

### Q: API 请求失败

**A:** 排查步骤：

1. **检查后端服务**
   ```bash
   # 确认后端服务运行
   curl http://localhost:3001/api/dashboard/stats
   ```

2. **检查网络配置**
   ```typescript
   // vite.config.ts 中的代理配置
   server: {
     proxy: {
       '/api': 'http://localhost:3001'
     }
   }
   ```

3. **查看浏览器控制台**
   - 检查网络请求状态
   - 查看错误信息
   - 确认请求 URL 正确

### Q: 样式显示异常

**A:** 可能的原因和解决方案：

1. **Tailwind CSS 未生效**
   ```bash
   # 检查 Tailwind 配置
   npx tailwindcss -i ./src/style.css -o ./dist/output.css --watch
   ```

2. **TDesign 样式冲突**
   ```css
   /* 调整样式优先级 */
   .custom-class {
     color: red !important;
   }
   ```

3. **CSS 模块问题**
   ```vue
   <style scoped>
   /* 使用 scoped 避免样式污染 */
   </style>
   ```

### Q: TypeScript 类型错误

**A:** 常见解决方案：

1. **更新类型定义**
   ```bash
   pnpm add -D @types/node @types/express
   ```

2. **配置路径别名**
   ```json
   // tsconfig.json
   {
     "compilerOptions": {
       "baseUrl": ".",
       "paths": {
         "@/*": ["src/*"]
       }
     }
   }
   ```

3. **忽略类型检查** (临时方案)
   ```typescript
   // @ts-nocheck
   ```

## 📞 获取帮助

如果以上解答无法解决您的问题，可以通过以下方式获取帮助：

1. **查看项目文档**
   - [README.md](../README.md)
   - [架构说明](./ARCHITECTURE.md)
   - [API 文档](./API.md)

2. **搜索相关资源**
   - [Vue 3 官方文档](https://vuejs.org/)
   - [TDesign 文档](https://tdesign.tencent.com/vue-next/overview)
   - [Vite 文档](https://vitejs.dev/)

3. **联系开发团队**
   - 创建 GitHub Issue
   - 发送邮件至开发团队
   - 在项目群组中提问

---

本 FAQ 文档会根据用户反馈持续更新，欢迎提出改进建议！